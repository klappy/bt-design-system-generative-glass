Wraps a whole screen to provide the signature soft-gradient backdrop.

```jsx
<AuroraField style={{width:390,height:844}}>{screen}</AuroraField>
```

There is no flat-colour screen background anywhere in this system. Lower `intensity` when a screen is card-heavy so the glass still reads.
