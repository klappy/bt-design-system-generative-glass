Soft dotted halo that marks the point new content grows from.

```jsx
<DotRing size={170}><Icon name="plus" size={22} color="var(--ink-400)"/></DotRing>
```

Density falls off outward and the whole ring breathes. Never use a spinner or progress bar instead — this is the system's loading and expand affordance.
