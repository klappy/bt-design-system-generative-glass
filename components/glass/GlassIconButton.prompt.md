Circular glass control for icon-only actions.

```jsx
<GlassIconButton label="Voice input"><Icon name="mic" size={22}/></GlassIconButton>
```

Hover adds the focus glow ring; press scales down. Use `tone="dark"` only for the single send button, `tone="night"` on map/spot screens.
