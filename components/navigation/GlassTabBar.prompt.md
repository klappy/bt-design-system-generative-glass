Phone tab bar as a floating glass pill.

```jsx
<GlassTabBar value={tab} onChange={setTab} items={[{id:'home',label:'Home',icon:'compass'},{id:'passages',label:'Passages',icon:'bookmark'},{id:'review',label:'Review',icon:'sparkle'},{id:'search',label:'Search',icon:'search'}]}/>
```
